{
  "name": "@brillout/part-regex",
  "version": "0.1.2",
  "description": "Create a RegExp by defining some parts with strings and other parts with RegExp",
  "main": "dist/partRegex.js",
  "scripts": {
    "test": "npm run build && node ./test.mjs",
    "build": "rm -rf dist/ && tsc",
    "prepublishOnly": "npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^15.12.2",
    "typescript": "^4.3.5"
  },
  "repository": "github:brillout/part-regex",
  "publishConfig": {
    "access": "public"
  }
}
